#include <stdio.h>
#include <stdlib.h>

int main()
{
    int hours_studied = 60; // 10 hours or more
    int kids_beat_up = 0;   // 0 kids 

    if ((hours_studied>=10) && (kids_beat_up==0)){
        printf("You're a good student \n");
    }


    return 0; 
}